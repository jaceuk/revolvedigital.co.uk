<div class="container">
	<slot />
</div>

<style lang="scss">
	.container {
		background-color: var(--c-glass);
		padding: var(--scale-16-64);
		border-radius: var(--border-radius-med);
		backdrop-filter: blur(var(--s-2));
		border: 1px solid var(--c-border);
	}
</style>
